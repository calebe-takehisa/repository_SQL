CREATE DATABASE LIBRARY 

USE LIBRARY;

CREATE TABLE BOOKS(
	BOOK_NAME VARCHAR(100),
	WRITER_NAME VARCHAR(100),
	WRITER_SEX CHAR(1)
	NUMBER_PAGES VARCHAR(4),
	PUBLISHER_NAME VARCHAR(30),
	BOOK_PRICE FLOAT(10,2),
	PUBLISHER_COUNTRY VARCHAR(30),
	PUBLICATION_YEAR INT (4)
);
